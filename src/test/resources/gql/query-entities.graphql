query {
    _entities(representations: [{ iban: "externalIban", currency: "externalCurrencyValue", __typename: "Account" }]) {
        ... on Account {
            currency
            iban
            balance
        }
    }
}