type Query {
    account(iban: String): Account!
}

type Account @key(fields: "iban") @extends {
    iban: String! @external
    userId: String! @external
    balance: String! @requires(fields: "userId")
}

